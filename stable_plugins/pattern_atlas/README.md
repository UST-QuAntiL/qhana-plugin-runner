# Requirements

1. Before running the plugin runner, ensure that the **Pattern Atlas API** is running, since this implementation no longer uses static files.
2. Start the plugin-runner using:
   ```bash
   poetry run flask run
